---
# Each domain points directly to its subdirectory in this repo.
# Git pull updates files in place - no copying needed.
# This file is required by cPanel but deployment is a no-op.
deployment:
  tasks:
    - /bin/echo "Deployed at $(date)"
